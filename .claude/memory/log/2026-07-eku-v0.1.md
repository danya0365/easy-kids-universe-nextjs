---
name: log-2026-07-eku-v0.1
description: บันทึกการ build v0.1 ของ Easy Kids Universe (โครงจักรวาล + 3 เกม + economy ร่วม) — อ่านตอน resume งานหรือดูว่าอะไรทำแล้ว/ค้าง
metadata:
  type: log
  status: active
  scope: global
---

# Easy Kids Universe — v0.1 build แรก (2026-07-05)

Build จักรวาลรวมมินิเกมเด็กเสร็จรอบแรก (client-only) ตาม plan `.claude/plans/...purring-cupcake.md`

## ทำเสร็จแล้ว

- **Phase 0:** ตัวตน Cosmo 🐼✨ + portable memory ใน repo (`.claude/`) + rules/commands/hooks + ADR-0001 + CLAUDE.md
- **Versioning:** footer `vX.Y.Z (sha)` จาก package.json + CHANGELOG/VERSIONING + release scripts · โลโก้ rename เป็น `logo.png` · แก้ site.webmanifest
- **Domain (pure + eslint gate):** ports (game/level/achievement/cosmetic) + Result + services (stars/unlock/energy/diamonds/achievements) — 5 unit test files
- **Data + adapters:** 13 เกม (3 playable), เนื้อหา abc/math/colors (5 ด่าน×5 รอบ), achievements 12, cosmetics 5, characters 4 + static adapters + factories + 4 contract test files
- **ธีม gen-3:** universe (default) / candy / galaxy — light+dark ครบ + game tokens (tile/star/energy/diamond) + `.app-bg`/`.chunky`/animations · Mali+Baloo2
- **Stores (zustand persist, key `eku-`):** progress/diamond/energy/achievement/settings/theme + toast (ไม่ persist)
- **Economy orchestrator** (`lib/economy.ts`): จบด่าน → saveStars → เพชร → energy bonus → evaluate achievements (cascade) → toast · daily gift
- **เกม:** engine ร่วม `use-round-engine` + registry (pluggable) + ABC (spelling) / Math / Colors + PlayGate (energy gate) + LevelComplete/Feedback/Pause
- **หน้า:** Home (grid 13) · /games/[gameId] (level map + teaser) · /play/[gameId]/[level] · /shop · /achievements · /settings · /how-to-play · 404

## Gate ที่ผ่าน (2026-07-05)

`npm run lint` ✅ · `npx tsc --noEmit` ✅ · `npm test` ✅ (54 tests) · `npm run build` ✅ (prerender 36 หน้า: 13 games + 15 play levels + static)
dev server: ทุก route 200, `/play/abc/9` + `/nope` → 404, ไม่มี hydration warning

## ยังไม่ได้ทำ / ค้าง (โอกาสต่อยอด)

- ⚠️ **ยังไม่ได้ verify interactive gameplay จริงบน browser** (แตะตัวอักษร/หักพลังงาน/ซื้อของ) — ตรวจได้แค่ SSR/build/prerender + unit test ของ logic · ต้องให้พี่ลองเล่นจริงบนมือถือ (โดยเฉพาะเสียง speech ที่ต้อง user gesture บน iOS)
- รูปคำศัพท์จริงยังไม่มี → fallback emoji (วางไฟล์ `/public/easy-kid-universe/words/<word>.png` ทีหลัง)
- รูปตัวละครจริงยังไม่มี → fallback emoji (`/public/easy-kid-universe/characters/<id>.png`)
- Backend (login Google + cloud save) = นอก scope v1 — ports คืน `Promise<Result<T>>` พร้อมเสียบ (ดู easy-abc plan 002)
- เกม coming-soon 10 ตัว (shapes/animals/... ) รอทำเนื้อหา — เพิ่มแค่ master + registry (core ไม่แตะ)
- `energy-empty-modal.tsx` มีแต่ยังไม่ได้ใช้ (PlayGate ใช้ full-screen card แทน) — เก็บไว้เผื่อ overlay ในเกม
